{
  "$schema": "https://deno.land/x/denon/schema.json",

  "scripts": {
    "start": {
      "cmd": "deno run --config .vscode/tsconfig.json server.ts",
      "desc": "run the main server.",
      "allow": ["net", "env", "read", "write"],
      "unstable": true
    }
  }
}
